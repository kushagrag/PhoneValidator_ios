# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'PhoneValidator' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod "SwiftValidators"
  pod "PhoneNumberKit"
  # Pods for PhoneValidator

  target 'PhoneValidatorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PhoneValidatorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
